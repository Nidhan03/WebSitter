# Secrets (never commit real API keys)
config.js

# OS / editor cruft
.DS_Store
*.log
.vscode/
.idea/

# Node (only relevant if any tooling gets added later)
node_modules/

# Chrome-generated when loading the extension unpacked
_metadata/

# Local manual test pages, not part of the extension
test-pages/
